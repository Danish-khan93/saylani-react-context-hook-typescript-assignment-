export type SIGNUP = {
  firstName: string;
  lastName: string;
  number: string;
  email: string;
  password: string;
  confirmPassword: string;
};

export type LOGIN = {
  email: string;
  password: string;
};
